{
  "name": "spane",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch run index.ts",
    "build": "bun build index.ts --outdir dist --target bun",
    "setup": "bun install && echo '✅ Dependencies installed. Make sure Redis is running on localhost:6379 or set REDIS_URL environment variable.'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bullmq": "^5.1.5",
    "elysia": "^1.4.16",
    "ioredis": "^5.3.2"
  }
}